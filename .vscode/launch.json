{    
    "version": "0.2.0",
    "configurations": [
        
        {
            "type": "chrome", 
            "name": "Launch Web Page", 
            "request": "launch", 
            "url": "http://localhost:8080", 
            "webRoot": "${workspaceFolder}" 

        }
    ]
}